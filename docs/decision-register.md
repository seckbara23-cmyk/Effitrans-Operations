# Effitrans — Decision Register

**Purpose:** the master, authoritative record of every architecture, business, security, hosting, workflow, and integration decision for the Effitrans Operations Platform. **All future changes go through this register** — a new dated row (or a status change to `Superseded`), never a silent edit elsewhere. The planning docs are downstream of this file.

**Status values** (full definitions in [§G Decision Status Standard](#g-decision-status-standard)): `Proposed` · `Approved` · `Implemented` · `Superseded` · `Rejected`. Intake items not yet formulated as a decision are tracked as `Open` in the [Blocker Register](s0-readiness-checklist.md#critical-blockers-register) until they enter the lifecycle.

**Change control:** every change follows [§F Change Control Process](#f-change-control-process). **Document impact** by decision category is mapped in [§H Document Dependency Matrix](#h-document-dependency-matrix).

**Categories:** Architecture · Business · Security · Hosting · Workflow · Integration · Data · Finance · Commercial.

Related: [s0-readiness-checklist.md](s0-readiness-checklist.md) · [architecture.md](architecture.md) · workshops in [docs/workshops/](workshops/).

---

## A. Architecture & product decisions (already directed / ratified)

These were directed by management across the discovery and planning prompts and are treated as **Approved** constraints development must follow.

| Decision ID | Category | Decision | Owner | Date | Status | Related Blocker | Documents Impacted |
|---|---|---|---|---|---|---|---|
| DEC-A01 | Architecture | Build a focused **logistics operations control tower, NOT a full ERP** | Management | 2026-06-13 | Approved | — | audit.md, requirements.md |
| DEC-A02 | Architecture | **Operational File** is the single source of truth; everything attaches to it | Management | 2026-06-13 | Approved | — | requirements.md, database-design.md |
| DEC-A03 | Architecture | Workflow driven by a **config-driven state machine** with role-gated transitions | Dev/Architect | 2026-06-13 | Approved | BLK-SM* | state-machine.md |
| DEC-A04 | Workflow | **Chief-of-Transit validation gate** + **POD hard gate** are non-negotiable | Management | 2026-06-13 | Approved | BLK-10 | state-machine.md |
| DEC-A05 | Architecture | Single **Next.js full-stack monolith** (no microservices) | Dev/Architect | 2026-06-13 | Approved | — | architecture.md |
| DEC-A06 | Architecture | **PostgreSQL via Supabase** (DB + Auth + Storage + scheduled jobs) — **platform APPROVED for Phase 1 foundation**. Rationale: *"Effitrans approved Supabase for Phase 1 foundation work."* Hosting **region** remains provisional pending BLK-9 (tracked by DEC-B09). | Dev/Architect + Management | 2026-06-13 (approved) | Approved | BLK-AR1 ✅ closed · BLK-9 (region) open → DEC-B09 | architecture.md |
| DEC-A07 | Data | Documents in **object storage**, never DB blobs | Dev/Architect | 2026-06-13 | Approved | — | architecture.md, database-design.md |
| DEC-A08 | Architecture | **French-first** UI (FR primary, EN secondary) | Management | 2026-06-13 | Approved | — | requirements.md |
| DEC-A09 | Business | **Tier-1 KPIs only** in Phase 1 (workflow-derived); financial/CSAT/employee KPIs deferred | Management | 2026-06-13 | Approved | — | requirements.md, audit.md |
| DEC-A10 | Business | **Phase boundaries**: no billing, GL/accounting, BI, or financial portal in Phase 1 | Management | 2026-06-13 | Approved | — | requirements.md, phase-1-roadmap.md |
| DEC-A11 | Business | **Minimal customer portal** = tracking + document upload + notifications only (P1) | Management | 2026-06-13 | Approved | — | requirements.md |
| DEC-A12 | Architecture | **Migration mechanism = Supabase CLI SQL migrations** (forward-only, plain SQL under `supabase/migrations/`), not an ORM migration tool. The typed query/ORM layer (supabase-js typed / Prisma / Drizzle) is a **separate, deferred** decision. | Dev/Architect | 2026-06-13 | Approved | — | database-design.md, s0-backlog.md, SETUP.md |

---

## B. Blocker-driven decisions (pending workshop confirmation)

One row per open blocker. `Proposed` = a documented default is in force; `Open` = no default, needs an answer. These flip to `Approved` when the relevant workshop closes them.

| Decision ID | Category | Decision (current) | Owner | Date | Status | Related Blocker | Documents Impacted |
|---|---|---|---|---|---|---|---|
| DEC-B28 | Architecture/Workflow | **Driver Mobile & Operations Execution (Phase 3.4C) APPROVED — dark by default.** Builds the driver mobile workspace (`/driver`) + dispatcher driver assignment on the 3.4A/B tracking foundation (DEC-B26). Increment 3.4C-1 (foundation) + 3.4C-2 (mobile UI) shipped; 3.4C-3 (delay/incident/photo/POD/delivery) + 3.4C-4 (dispatcher live console/map, **polling-first**; Realtime later) deferred. **Reuses** the one auth (DRIVER on app_user), one transport state machine, one tracking-session model, one POD/Finance-handoff, one notification + audit system — no second workflow. **Tracking stays evidence** (DEC-A02): start/pause/resume/stop + GPS batches NEVER mutate transport status, customs, invoice, payment, lifecycle, or delivery; delivery stays the controlled transition; geofence entry never auto-delivers. **Driver identity/routing:** DRIVER lands on `/driver` (`postLoginPath`), is kept off staff pages, never sees finance/customs/admin/other drivers. **Migration 20260711000001** (additive): unique active-session-per-transport (idempotent start), additive `transport_record` driver SELECT policy (tenant-scoped), `tracking_event.detail` jsonb, `tracking_position.idempotency_key`. **Security:** consent-gated `watchPosition` (only during an active mission, after explicit consent + flag; never on load); bounded offline queue (idempotency keys, never "sent" before server confirm, no dossier data stored); secure batch endpoint that derives tenant/file/transport/driver from the SESSION (never client-supplied); positions append-only (service-role writes only); batch acceptance audited, not per point; RLS tests for driver/finance/cross-tenant isolation. **Dark:** `TRACKING_ENABLED` + `DRIVER_MOBILE_TRACKING_ENABLED` gate the surface; lifecycle portal unaffected. **Privacy/retention** pending stakeholder/legal approval (raw positions 90d proposal; no auto-deletion). No client-facing live tracking, no paid maps, no external GPS/GAINDE/ORBUS, no AI route prediction, no port-11434 exposure. | Management/IT + Dev | 2026-07-11 | Approved (3.4C-1/2); driver pilot pending privacy review | privacy review open | phase-3.4c-driver-execution.md, database-design.md, rbac-matrix.md, s2-security-patterns.md |
| DEC-B27 | Architecture/Integration | **Provider-neutral AI for the Operations Copilot (Phase 3.4F-1) APPROVED.** Replaces the single paid-OpenAI dependency with a common `AIProvider` interface (`lib/ai/*`) supporting **OpenAI / Ollama / vLLM** (Qwen preferred locally, Llama as an alternative). Switching provider is **configuration** (`AI_PROVIDER`/`AI_MODEL`/`AI_BASE_URL`/`AI_API_KEY`), not a Copilot rewrite. **Backward compatible:** `AI_PROVIDER` unset ⇒ existing OpenAI behaviour via `OPENAI_API_KEY`/`OPENAI_COPILOT_MODEL` (prod unchanged). **Preserves the read-only model:** no tools/function-calling, no DB writes, no task/email, no autonomous actions; the model receives only the existing permission-filtered context. **Reliability/security:** timeout + one bounded retry, prompt/response size caps, EXPLICIT-only fallback (no silent paid fallback), secret-free logs, and hosted-production (Vercel) rejection of localhost base URLs, plain-HTTP remote endpoints, unauthenticated remote private endpoints, and off-allowlist hosts (with internal-network overrides). **Local providers DARK BY DEFAULT** (`AI_LOCAL_PROVIDER_ENABLED=false`). Adds a deterministic evaluation harness over sanitized fixtures + an admin-only `/api/ai/health` (secret-free). **No model version hard-coded** (`AI_MODEL` supplies it; a model is not "suitable" until it passes Effitrans evaluation). **Constraints kept:** no training/fine-tune, no RAG/vector DB, no AI actions, portal Copilot not connected, OpenAI support retained. | Management/IT + Dev | 2026-07-10 | Approved | — | phase-3.4f-1-local-ai-providers.md, architecture.md |
| DEC-B26 | Architecture/Security | **Real-time operations tracking (Phase 3.4) APPROVED — dark-by-default evidence layer.** Adds a provider-neutral tracking model (`tracking_session` / append-only `tracking_position` / `tracking_event`) OVER the existing transport lifecycle — never a second status source (DEC-A02): a position/event is **evidence only** and NEVER auto-transitions a dossier or moves finance (no DELIVERED-from-geofence, no invoice from GPS). **Driver identity (locked):** a narrowly-scoped **DRIVER role on the EXISTING `app_user`/auth** (DEC-B12) + additive `transport_record.driver_user_id`; RLS scopes a driver to their own assigned transports (`is_assigned_driver`), free-text `driver_name` kept for non-app drivers. **Flags (all OFF):** `TRACKING_ENABLED` master + `DRIVER_MOBILE_TRACKING_ENABLED` / `PORTAL_LIVE_TRACKING_ENABLED` / `TRACKING_REALTIME_ENABLED` / `TRACKING_GEOFENCE_ENABLED`; with the master off the lifecycle portal is unchanged. **Shipped 3.4A/B:** schema + RLS (staff / assigned-driver / portal-customer-visible) + `tracking:*` perms + manual ops updates + internal event timeline + ETA v2 (confidence degrades on stale GPS, never fabricates a date). **Deferred (later 3.4 increments, still dark):** driver mobile app + browser geolocation, portal live map, Supabase Realtime, geofence worker, external carrier/vessel/flight providers, retention cron. **NEW PRIVACY SURFACE — needs review before the driver pilot:** location is collected ONLY during an active assignment, with explicit driver consent; no background/idle tracking; **Senegal data-privacy + employment-policy review required** before enabling `DRIVER_MOBILE_TRACKING_ENABLED`; **retention proposal for approval:** raw positions **90 days**, simplified route/events with the dossier, POD/audit per existing policies (no destructive cleanup cron until approved — cf. DEC-B19). | Management/IT + Dev | 2026-07-10 | Approved (3.4A/B); driver-pilot & retention pending privacy review | BLK-1 (carrier APIs) · privacy review open | phase-3.4-realtime-tracking.md, database-design.md, rbac-matrix.md, s2-security-patterns.md |
| DEC-B01 | Integration | GAINDE/Orbus handled by **manual reference-tracking** until a real API is confirmed | IT + Chief of Transit | — | Proposed | **BLK-1** | architecture.md, database-design.md |
| DEC-B25 | Security/Auth | **Google OAuth Sign-In (Phase 1.16) APPROVED — staff-first.** Adds "Continue with Google" to the internal `/login` via Supabase Auth Google provider (PKCE), with **NO open self-registration**. **Locked:** no hard Workspace-domain restriction for now; **`app_user`/`client_user` id-based profile gating stays the authority** (gate keys on `auth.users.id`, never an email lookup; email match is an *additional* assertion against the **verified** Google email); **Google enabled for BOTH staff and portal** (Q3, updated from staff-first); rejected unknown/orphan users are **signed out AND their orphan `auth.users` row deleted** (admin); **no auto-creation** of profiles; **email stays invite/admin-managed** (no self-service change). Flow: `/auth/callback` route handler → `exchangeCodeForSession` → gate (active `app_user` for this id + email match) → `/dashboard`, else signOut + orphan-delete → `/login?error`. Audit `auth.login.google` (attributed) + `auth.login.rejected` (machine event, null actor, reason only — no plaintext email). Supabase config: Google provider on, **signups OFF**, verified-email linking ON, redirect-URL allowlist. **Preserves** RLS + audit; no schema/migration. **Also in 1.16 — staff password recovery:** "Mot de passe oublié" on `/login` triggers Supabase `resetPasswordForEmail` (PKCE, browser-initiated so the verifier is local) → `/auth/update-password` exchanges the code, **gates the recovery session to an ACTIVE `app_user` by id** (`assertStaffRecovery` — portal/inactive/orphan refused, signed out), then `updateUser({password})` → signOut → `/login?reset=success`. **Unknown emails get a generic success message (anti-enumeration)**; the request audit (`auth.password_reset.requested`) is internal and **only emitted for an active staff email** (no leak); completion audited `auth.password_reset.completed`. No auto-profile creation; **no self-service email change**. **Portal parity (Q3):** portal users get the **same** Google sign-in (`/portal/auth/callback` → `client_user`-by-id gate, INVITED→ACTIVE on first Google login, DISABLED/non-portal/orphan refused + orphan `auth.users` deleted) and password reset (`/portal/auth/update-password`); audit `portal.login.google` / `portal.login.rejected` / `portal.password_reset.requested|completed`. A staff account reaching the portal callback (has `app_user`, no `client_user`) is rejected but **never deleted** (and vice versa). **Deferred:** Workspace `hd` hard restriction, self-service email change. | Management/IT + Dev | 2026-06-15 | Approved | — | phase-1.16-google-oauth-plan.md, architecture.md |
| DEC-B24 | Finance/Integration | **Real Payment Provider Integration (Phase 1.15B) APPROVED — scaffold only, no live money.** Adds a **`payment_intent`** table (orchestration; NOT a money row) + append-only **`provider_webhook_event`** table, a server-only **provider abstraction** (`MockProvider` full; **Wave / Orange Money placeholders** returning `not_configured`), and a webhook **Route Handler scaffold**. Behind master flag **`PAYMENTS_ENABLED=false`**. **Locked decisions:** (Q1) **Architecture + Mock first; Wave first when credentials land; Orange Money after Wave.** (Q2) **Staff-generated payment links first**; portal **Pay button ships behind the flag, disabled**. (Q3) **No partial online payments** — intent amount = full invoice balance. (Q4) **Provider success auto-verifies** — when signature + idempotency + non-replay + tenant/invoice match + amount-equals-balance + invoice-payable ALL pass, **auto-create a `payment` born `VERIFIED`** (`payment.auto_recorded`). (Q5) **TTL expiry** → EXPIRED; **failed/expired intents stay visible in reconciliation**. (Q6) Webhook **records directly ONLY** after all §4.5 guards pass; otherwise log the event, mark intent **FAILED/UNMATCHED**, route to manual reconciliation. **Preserves** the 1.11 paid/balance formula (Σ non-reversed) — only a SUCCEEDED intent spawns a normal `payment`; RLS finance-gated like `payment` + additive portal read; append-only audit. **Deferred (no code):** real credentials, live API calls, bank-transfer API, refunds/disputes/chargebacks, payouts, recurring, partial online payments. | Management/IT + Dev | 2026-06-15 | Approved | — | phase-1.15b-payment-provider-plan.md, rbac-matrix.md, database-design.md |
| DEC-B23 | Finance | **Payment Recording Integrations (Phase 1.15A) APPROVED — manual only.** Extends the 1.11 `payment` table (additive) with manual provider metadata (`provider_name`, `provider_reference`, `received_by`) and a one-shot reconciliation workflow `verification_status` PENDING→VERIFIED/REJECTED (`verified_by`/`verified_at`/`verification_note`). **No new permission** — VERIFY/REJECT reuse **`finance:void`**; reference edits reuse `finance:update`. **Reject = reverse + mark REJECTED**: rejecting also sets `reversed_at`, so the row leaves the paid total and the **1.11 paid/balance formula (Σ non-reversed) and all invoice calculations are unchanged**. New `/finance/reconciliation` view (gated `finance:read`) surfaces unverified payments, missing-reference flags, recently resolved, and outstanding balances. Audit `payment.verified`/`payment.rejected` added. Portal stays **read-only** (no payment buttons/collection). **Deferred (no code):** Wave API, Orange Money API, webhooks, online gateways, payment collection — the schema is shaped to accept them later. Realizes DEC-B01-style manual tracking for payments. | Management/IT + Dev | 2026-06-15 | Approved | — | phase-1.15a, rbac-matrix.md, database-design.md |
| DEC-B02 | Data | Capacity sized for **65 internal users + moderate volume**; revisit on real numbers | Operations/Mgmt | — | Open | **BLK-2** | architecture.md |
| DEC-B03 | Workflow | Document-type catalog + validity periods + block-vs-warn **per Chief of Transit**. **MVP slice Approved via DEC-B21**; the full customs expiry-bearing catalog (APE/DPI/exonération/sommiers) + per-type validity *values* remain pending Chief of Transit, deferred to the Customs module. | Chief of Transit | 2026-06-14 (MVP) | Approved (MVP) | **BLK-3** | document-catalog.md, blk-3-documents-governance.md, state-machine.md |
| DEC-B04 | Workflow | Notification matrix default = **email+SMS to AM + next-action role, immediate** | Operations | — | Proposed | BLK-4 | requirements.md |
| DEC-B05 | Integration | **WhatsApp deferred to Phase 2**; Phase 1 = email + SMS | Management | — | Proposed | BLK-5 | requirements.md |
| DEC-B06 | Data | **File-numbering scheme APPROVED:** `EFT-{TYPE}-{YEAR}-{SEQUENCE}` — prefix `EFT`; TYPE ∈ IMP/EXP/TRP/HND; 4-digit year; **5-digit zero-padded** sequence scoped **per tenant × type × year**; numbers never reused; assigned on file creation; branch code deferred; concurrency-safe generation required. e.g. `EFT-IMP-2026-00001`. | Operations | 2026-06-14 (approved) | Approved | BLK-6 ✅ closed | database-design.md, phase-1.2 |
| DEC-B07 | Architecture | Tolerate intermittent field/port connectivity; offline depth **TBD** | Operations | — | Open | BLK-7 | requirements.md |
| DEC-B08 | Data | Migration = **clean start + legacy read-only archive** | Management | — | Proposed | BLK-8 | phase-1-roadmap.md |
| DEC-B09 | Hosting | Hosting region/model pending **client data-restriction** confirmation; default managed cloud | Management + clients | — | Open | **BLK-9** | architecture.md |
| DEC-B10 | Workflow | POD acceptance = signed consignee receipt, **AM-validated**; exact evidence TBD | Operations (CoT/AM) | — | Open | BLK-10 | state-machine.md, requirements.md |
| DEC-B11 | Security | Role list (13–15) maps to real Effitrans functions — to confirm | Operations/Mgmt | — | Open | BLK-RB1 | rbac-matrix.md |
| DEC-B12 | Security | **One named IT System Admin + one break-glass backup**; no shared admin accounts | Management/IT | — | Open | BLK-RB2 | rbac-matrix.md |
| DEC-B13 | Security | Multi-role users receive the **union** of their roles' permissions | IT/Dev | — | Proposed | BLK-RB3 | rbac-matrix.md |
| DEC-B14 | Security | Coordinator "team/zone" scope definition (geo/client/org-unit) — TBD | Operations | — | Open | BLK-RB4 | rbac-matrix.md, database-design.md |
| DEC-B15 | Security | Whether Account Managers can see peers' files — default **own + assigned clients only** | Operations/Mgmt | — | Proposed | BLK-RB5 | rbac-matrix.md |
| DEC-B16 | Security | Identity = **standalone Supabase Auth**, MFA for admins/external (vs SSO) | IT | — | Proposed | BLK-DB1, BLK-AR3 | rbac-matrix.md, architecture.md |
| DEC-B17 | Data | Client/partner entity overlap handling — default **separate entities** | Operations/Dev | — | Proposed | BLK-DB2 | database-design.md |
| DEC-B18 | Finance | Multi-currency in Phase 1 — default **XOF only**, multi-currency TBD | Finance/Mgmt | — | Open | BLK-DB3 | database-design.md |
| DEC-B19 | Data | Archived-document **retention policy** (Senegal legal minimum) — TBD | Mgmt/Compliance | — | Open | BLK-DB4 | database-design.md |
| DEC-B22 | Security | **Customer Portal security model (Phase 1.12) APPROVED.** A SECOND identity class on the **same Supabase Auth project**: staff via `app_user`, external clients via **`client_user`** (a given `auth.users` id is in exactly one). Portal access is **strictly client-scoped** via **additive** RLS policies keyed on `client_user.client_id` (OR'd with staff policies — staff RLS unchanged); internal policies deny portal users automatically (no `app_user` → no permission/tenant). Portal reads use the **user-context client** with **safe column projections**; service-role only for the gated **invite/manage** and (1.12B) **document-download** actions. Portal sees **only its own client's** dossiers/shipment/customs+transport **summary**; **never** tasks, audit, internal notes, charges, drafts. Staff invite/manage portal users (`portal:manage`); `client_user.status` INVITED/ACTIVE/DISABLED, `role` CLIENT_ADMIN/CLIENT_USER (portal-local, **not** RBAC). Audit attributed via nullable `audit_log.client_user_id`. Docs default **not shared** (`shared_with_client`, 1.12B). Separate `/portal/login`. Deferred: client uploads, online payments, messaging, multi-client users, branding. | Management/IT + Dev | 2026-06-15 | Approved | — | phase-1.12, rbac-matrix.md, architecture.md |
| DEC-B21 | Workflow | **Documents MVP governance (BLK-3 slice) APPROVED** (recommended defaults D1–D8): (D1) editable 10-type catalog — BL, AWB, Commercial Invoice, Packing List, Certificate of Origin, Customs Declaration, Delivery Note/POD, Transport Order, Payment Receipt, Other; (D2) only Certificate of Origin expires in MVP, **warn-only** (no transition block); (D3) "required" documents **flag/warn**, do not block dossier progression; (D4) `document:approve` = SYSTEM_ADMIN, OPS_SUPERVISOR, ACCOUNT_MANAGER, CHIEF_OF_TRANSIT, COMPLIANCE_HSSE; (D5) re-upload = **new version row** (history kept); (D6) **soft-delete only** (hard-delete pending retention DEC-B19); (D7) ≤ 25 MB, MIME pdf/jpg/png/docx/xlsx; (D8) **private bucket + server-mediated short-TTL signed URLs, no public URLs**. Documents inherit Phase-1.7 dossier visibility (no `document:read:all`). EXPIRED is derived (no scheduler); `document.expired` audit + reminders deferred. | Chief of Transit + Management/IT | 2026-06-14 | Approved | BLK-3 (MVP) | blk-3-documents-governance.md, document-catalog.md, rbac-matrix.md |
| DEC-B20 | Workflow | **Phase 1.6 in-app notifications:** durable **self-scoped** `notification` table (RLS `user_id = auth.uid()` + tenant; **no new RBAC permission** — visibility is the recipient identity). `TASK_ASSIGNED` generated on task assignment (change-only, never self); overdue / due-today are **derived** (not stored). **Scheduled reminders (`TASK_DUE_SOON`/`TASK_OVERDUE`) and the email/SMS provider are DEFERRED** — dispatch hook is a no-op stub behind `NOTIFICATIONS_EMAIL_ENABLED`. Incrementally realizes DEC-B04/DEC-B05. **No new audit events** (reuses `TASK_ASSIGNED`). | Dev/Architect | 2026-06-14 | Approved | BLK-4 (partial) | requirements.md, phase-1.6, rbac-matrix.md |
| DEC-B59 | Architecture | *(formerly DEC-B23, HR series — renumbered 2026-07-25 to resolve a duplicate-id collision with the 2026-06 platform block; docs/hr/* still cite the original id)* **HR bounded context APPROVED** (HR-0 audit ratification): a dedicated `employee` record separate from authentication identity (`auth.users`), tenant membership (`app_user`), and org metadata (departments registry). **Employees without platform accounts are supported.** Optional employee↔app_user link: tenant-matched, at most 1:1, **grants nothing**, unlinking deletes nothing; `/users` remains the accounts surface and is never repurposed. | Management | 2026-07-24 | Approved | — | docs/hr/hr-0-architecture-audit.md, docs/hr/hr-1-implementation-plan.md |
| DEC-B60 | Architecture | *(formerly DEC-B24, HR series — renumbered 2026-07-25)* **HR-1 route & navigation:** HR lives at **`/departments/hr`** (the Direction pattern — MANAGEMENT items point at `/departments/*` routes); sidebar item **« Ressources humaines » under MANAGEMENT**, cosmetically gated `hr:read`. HR is never added to the DÉPARTEMENTS section; the canonical department registry (incl. HUMAN_RESOURCES) is unchanged. | Management | 2026-07-24 | Approved | — | docs/hr/hr-0-architecture-audit.md, docs/hr/hr-1-implementation-plan.md |
| DEC-B61 | Security | *(formerly DEC-B25, HR series — renumbered 2026-07-25)* **HR authorization:** new permissions **`hr:read`** (directory + employment data) and **`hr:manage`** (create/update/lifecycle/link) only; new role **`HR_OFFICER`** holding both (+ baseline), mapped to canonical HUMAN_RESOURCES; HR_MANAGER deferred. **SYSTEM_ADMIN receives NO `hr:*` permission by default** — a deliberate exception to the full-admin grant convention (precedent: `process:override` granted to no role). Job title/department never grant permissions. | Management | 2026-07-24 | Approved | — | docs/hr/hr-0-architecture-audit.md, rbac-matrix.md |
| DEC-B62 | Workflow | *(formerly DEC-B26, HR series — renumbered 2026-07-25)* **Employment lifecycle:** `DRAFT → ACTIVE → (SUSPENDED ⇄ ACTIVE) → TERMINATED → ARCHIVED` as a pure transition table; **ON_LEAVE is derived from leave records (HR-3), never a stored status**; SUSPENDED = employment suspension only (platform access stays `/users` + auth-ban); **rehire = a new employment record** (terminated records stay queryable, never reactivated); **termination and account revocation remain separate actions** — termination may *prompt* the existing archive/ban flow, never trigger it silently. | Management | 2026-07-24 | Approved | — | docs/hr/hr-0-architecture-audit.md |
| DEC-B63 | Data | *(formerly DEC-B27, HR series — renumbered 2026-07-25)* **HR deferrals & boundaries:** assignment history (`employee_assignment`) → HR-2 (HR-1 relies on audited before/after); personnel documents + dedicated private `hr-documents` bucket + `hr:documents:*` → HR-2; manager access to direct reports → HR-3+ (`manager_employee_id` is display-only in HR-1); employee self-service → HR-8; **compensation = a separately-restricted HR domain (`hr:compensation:*`) interfaced to Finance for payroll (HR-7)** — no salary/national-ID/medical field ever on `employee`; all Senegal-specific legal rules (contract types, leave, IPRES/CSS/NINEA identifiers, retention) require legal confirmation before HR-2+ encodes them. Audit payloads carry safe metadata only — never salary/medical/ID/emergency-contact/contract content. | Management | 2026-07-24 | Approved | DEC-B19 (retention) | docs/hr/hr-0-architecture-audit.md |
| DEC-B64 | Architecture | **WES-0 canonical workflow architecture RATIFIED** (docs/workflow/wes-0-canonical-workflow-architecture.md). The WES Audit found six engines, five progress computations, seven assignment slots and no arbiter. Ratified: ONE authority chain — external evidence -> module records (evidence layer) -> process engine (sequencing authority) -> canonical lifecycle projection -> all UI; modules never command the engine, the engine consumes evidence. Four concepts permanently separated: ownership (accountability) != assignment (who works now) != visibility (who may see) != responsibility (which department must act). Departments own dossiers, people own tasks, drivers own missions. Monotonic lifecycle: blockers are overlays, never regressions; the only decrement is an explicit reasoned supervisor reversal. ONE progress formula over the canonical 20 stages. BAE = external evidence Effitrans VERIFIES, never approves (record: CUSTOMS_FIELD_AGENT - verify: CHIEF_OF_TRANSIT - release: the pickup gate - override: SYSTEM_ADMIN reasoned). Includes ADR-WES-001..011. | Management | 2026-07-26 | **Approved** | — | wes-0-canonical-workflow-architecture.md |
| DEC-B65 | Architecture | **ADR-WES-012 — business rules as VERSIONED CONFIGURATION.** The engine is reusable software; business policy is versioned configuration. CODE keeps the invariants (state-machine mechanics, CAS/idempotency, maker-checker IDENTITY, tenant isolation/RLS, audit+event emission, monotonicity, the evidence and permission FRAMEWORKS, the single projection/progress formula, append-only semantics). CONFIGURATION covers seven domains: stage registry - applicability - seat bindings (incl. BAE seats, handoff/escalation recipients, supervisor intervention) - evidence requirements - maker-checker PAIRS - SLA policies - handoff routing. Versions are IMMUTABLE with effective dates; platform default + tenant overrides; **a dossier PINS its policy version at process-instance creation**, so policy changes never affect in-flight dossiers except by an explicit audited migration. Every step execution, assignment and event records the policy version in force — a dossier must retain enough provenance to explain why an action was allowed at the time. Governance: dedicated capability distinct from admin:config:manage, maker-checker activation (author != activator), MANDATORY preview/dry-run, rollback as a NEW version. Eight safety boundaries validated FAIL-CLOSED at activation (no RLS bypass, no permission outside the catalog, no audit disabling, no maker=checker, no illegal transition, no monotonicity break, no emptying doctrine-mandated evidence, no non-existent seat). | Management | 2026-07-26 | **Approved** | — | wes-0a-ratification-addendum.md §1 |
| DEC-B66 | Architecture | **ADR-WES-013 — canonical SLA, time and escalation model.** Canonical timestamp vocabulary (available/assigned/accepted/started/due/completed, paused+blocked seconds, elapsed working time, SLA status) on every SLA-bearing subject: process steps, tasks, handoffs, missions, document verification, client requests, finance approvals. Storage UTC; ALL business-time computation uses the TENANT timezone (organization.timezone) via the ratified 10.0D window mechanism — server-local and browser-local time are PROHIBITED. Business calendars are configuration, versioned and pinned; **departments may hold different calendars** (24-hour transport/customs vs business-hours finance). FOUR clocks always recorded: total elapsed (never paused, **never hidden**) - internal accountable (what SLA measures) - external waiting (client/Customs/carrier — excluded from internal SLA, reported separately) - paused (outage/deliberate, flagged). Blocking internally does NOT pause internal SLA. Escalation warning->breach->management with dedup (one per subject/level/window, tracking_event dedup_key pattern); escalations are business events. The existing unconfigured/unratified/ratified tri-state becomes platform law: unconfigured NEVER produces overdue, and the four live-but-unratified thresholds in lib/sla/config.ts must be explicitly ratified or retired in WES-8. | Management | 2026-07-26 | **Approved** | four unratified SLA thresholds (resolve in WES-8) | wes-0a-ratification-addendum.md §2 |
| DEC-B67 | Data | **ADR-WES-014 — immutable business event ledger.** ONE append-only cross-domain operational timeline + integration stream, populated from successful committed domain actions. Events are appended, never edited, never deleted; corrections are later events. **Existing domain ledgers remain authoritative for their domain; where they disagree the domain ledger wins.** audit_log is NOT replaced (different purpose, retention and privacy class). Ratified envelope incl. actor_role_at_time (frozen at write), responsible_department, process/step ids, source_subsystem, correlation_id, causation_id, policy_version, override marker+reason. **TRANSACTIONALITY: best-effort event writes are FORBIDDEN.** The service-role client cannot hold a multi-statement transaction, so app-layer dual writes are prohibited; approved mechanisms are RPC-first (security definer function writes domain row + event in one call — the provision_tenant/next_*_number precedent) or trigger-based emission where every field is row-derivable. If an event cannot be emitted atomically with its domain fact, the domain action FAILS. Prohibited payloads: document contents, OCR text, message/email bodies, amounts and banking details, PII beyond identifiers, credentials/tokens/signed URLs, AI prompts, raw form dumps. Client-safe feed is an ALLOW-LIST projection, never a filter over full rows. The ledger never authorizes an action and never replaces the process engine. | Management | 2026-07-26 | **Approved** | statutory retention values (DEC-C24, external legal) | wes-0a-ratification-addendum.md §3 |
| DEC-B68 | Architecture | **ADR-WES-004/005 refinements + phase sequencing.** (a) **Mission persistence is REUSE-FIRST:** the Mission bounded concept stays mandatory, but WES-0's implied new `transport_mission` table is WITHDRAWN as a physical decision taken without repository analysis. A written reuse-vs-new-entity analysis over transport_record / tracking_session / driver structures / tasks / existing constraints is REQUIRED before any mission schema work; a dedicated entity is justified only if the existing model cannot provide all ten listed capabilities (incl. one dossier->many missions, at-most-one-active, driver-USER assignment, reassignment without recreating execution state). Recorded evidence: transport_record.file_id is UNIQUE NOT NULL (blocks multi-mission) and tracking_session already carries file/transport/driver + a 4-state lifecycle. (b) **Category-B documents refined:** internal operational documents are system-generated IMMUTABLE artifacts derived from authoritative structured data — never manually uploaded or edited; corrections are made by changing the structured record through an authorized action and REGENERATING; each artifact records source-data version, artifact version, content hash, generated-by identity, supersession; reuses the 11.0B/C deterministic-renderer + content_sha256 + immutable-version machinery. (c) **Sequencing:** three NEW phases — WES-7 Policy Registry, WES-8 SLA Engine, WES-9 Business Event Ledger — rather than inflating WES-1..6. Execution order: WES-1 -> WES-2 -> **WES-7** (must precede WES-3/4, which write seat bindings) -> **WES-9** (must precede WES-5, the highest-volume event producer) -> WES-3 -> WES-4 -> WES-5 -> WES-8 -> WES-6. | Management | 2026-07-26 | **Approved** | — | wes-0a-ratification-addendum.md §4-6 |

### Phase 10.0 — Centre d'Opérations series (ratified in the 10.0B/10.0D-1/10.0E-1 phase authorizations; entered 2026-07-25)

| Decision ID | Category | Decision | Owner | Date | Status | Related Blocker | Documents Impacted |
|---|---|---|---|---|---|---|---|
| DEC-B29 | Architecture | **/dashboard remains the single Centre d'opérations, evolved in place** — no new/parallel cockpit route; frozen sidebar preserved. | Management | 2026-07-24 | Implemented (10.0C) | — | docs/operations/phase-10.0a-operations-cockpit-architecture.md |
| DEC-B30 | Security | **Named per-person workload only behind the existing supervision boundary (`analytics:read`)**; aggregated dept/team rows otherwise; coordination data, never a performance score; no new permission. | Management | 2026-07-24 | Implemented (10.0B) | — | phase-10.0a §13/§27 |
| DEC-B31 | Architecture | **No Supabase Realtime; no uncontrolled polling** — request-driven reads + cache() + Suspense + explicit refresh only. | Management | 2026-07-24 | Implemented | — | phase-10.0a §14/§27 |
| DEC-B32 | Security | Operations-Copilot permission deferred to 10.0F. | Management | 2026-07-24 | **Superseded** — 10.0F (2026-07-25) reused the EXISTING `logistics:copilot:read`; no new permission was ever created. | — | phase-10.0a §27; lib/operations/copilot/context.ts |
| DEC-B33 | Architecture | **`getExecutiveAnalytics` quarantined** — legacy `/analytics`-only stack; no new Centre d'opérations dependency may use it (test-pinned); retirement is a separate future approval. | Management | 2026-07-24 | Implemented (pins) | — | phase-10.0a §27 |
| DEC-B34 | Data | **Additive optional stable alert `code` field** — machine-readable, stable across French label changes. | Management | 2026-07-24 | Implemented (10.0E-1 `AlertCode`) | — | phase-10.0a §27; lib/operations/alerts/codes.ts |
| DEC-B35 | Architecture | **One authoritative KPI engine** (`lib/operations/kpi/`) composing owning readers; CT/cockpit/exécutif/reports = consumers. | Management | 2026-07-24 | Implemented (10.0D) | — | docs/operations/phase-10.0d-executive-kpi-engine-architecture.md |
| DEC-B36 | Security | Executive KPI strip gated **`analytics:read`**; monetary KPIs additionally **`finance:read`** (absent otherwise, never zero). | Management | 2026-07-24 | Implemented (10.0D-1/4) | — | phase-10.0d §26 |
| DEC-B37 | Data | **Initial executive KPI set** (8 primary + 1 conditional) as catalogued. | Management | 2026-07-24 | Implemented (10.0D-1..4) | — | phase-10.0d §14 |
| DEC-B38 | Data | **Windows limited to Today + Month-to-Date** (prev-full-month as comparison basis); start-inclusive/end-exclusive. | Management | 2026-07-24 | Implemented (10.0D-2) | — | phase-10.0d §6 |
| DEC-B39 | Data | **All business-day boundaries resolve in `organization.timezone`** (default Africa/Dakar); UTC business logic forbidden. | Management | 2026-07-24 | Implemented (10.0D-2) | — | phase-10.0d §6; lib/operations/kpi/windows.ts |
| DEC-B40 | Finance | **Every monetary KPI computed per currency** — never a cross-currency scalar, never implicit conversion. | Management | 2026-07-24 | Implemented (10.0D-3) | — | phase-10.0d §9 |
| DEC-B41 | Data | Prior value 0/null ⇒ comparison `direction:"unknown"`, `changePercent:null` — never fabricated growth. | Management | 2026-07-24 | Implemented (10.0D-3) | — | phase-10.0d §26 |
| DEC-B42 | Data | **Snapshot trends deferred** until real snapshot history exists; flow trends allowed from event timestamps. | Management | 2026-07-24 | Approved | — | phase-10.0d §8 |
| DEC-B43 | Workflow | **Active dossier = any dossier not in a terminal state (CLOSED, CANCELLED; ARCHIVED when it exists)** — DRAFT and DELIVERED are active (business revision of the audit proposal). ONE canonical predicate (`isActiveFileStatus`) used everywhere; 7 former duplicate definitions delegated. | Management (revised) | 2026-07-24 | Implemented (10.0D-1) | — | phase-10.0d §26; lib/files/status.ts |
| DEC-B44 | Finance | **« Facturé (mois) » (invoice issue) and « Encaissé (mois) » (payments) are two distinct KPIs**; the ambiguous « Revenu du mois » label retired. | Management | 2026-07-24 | Implemented (10.0D-3/4) | — | phase-10.0d §26 |
| DEC-B45 | UX | Freshness label = truthful « Dernière actualisation : <timestamp> »; per-KPI freshness `live-request`; no “live/temps réel” claims. | Management | 2026-07-24 | Implemented | — | phase-10.0d §26 |
| DEC-B46 | Data | **Data quality: exclude invalid rows + basis counts + `partial` status; `unavailable` on dark/failed sources** — never false precision, never a crashed section. | Management | 2026-07-24 | Implemented (10.0D) | — | phase-10.0d §21 |
| DEC-B47 | Architecture | **Existing executive normalize/merge engine stays authoritative for alerts**; thin `lib/operations/alerts` wrapper adds the code-aware contract — never a replacement. | Management | 2026-07-24 | Implemented (10.0E-1) | — | docs/operations/phase-10.0e-unified-alert-center-architecture.md §27 |
| DEC-B48 | Data | **Alerts computed-only** (active while source condition holds); acknowledge/dismiss/assign deferred (would need schema + a second state). | Management | 2026-07-24 | Implemented | — | phase-10.0e §27 |
| DEC-B49 | Security | **No new alert permission** — per-adapter SOURCE permissions; missing ⇒ omitted (absent ≠ zero). | Management | 2026-07-24 | Implemented (10.0E-2) | — | phase-10.0e §27 |
| DEC-B50 | Data | Normalized alert severity = the existing 4-level executive model (critical/high/medium/low) + documented adapter mappings; no overdue→critical inflation. | Management | 2026-07-24 | Implemented | — | phase-10.0e §27 |
| DEC-B51 | Data | Stable alert code convention **`domain.entity.condition`**, adapter-owned typed unions; existing shipping/air codes mapped 1:1; level-based risk codes v1. | Management | 2026-07-24 | Implemented (10.0E-1) | — | phase-10.0e §27 |
| DEC-B52 | Data | Dedupe key **code+entityType+entityId** (legacy origin\|reference\|reason fallback); highest severity wins, earliest timestamp kept, no description merging. | Management | 2026-07-24 | Implemented (10.0E-1) | — | phase-10.0e §27 |
| DEC-B53 | Architecture | **Initial alert adapter set (7)**: risk findings, Command-Center pass-through, finance requests, reconciliation, overdue receivables, failed communications, messaging urgent/waiting. | Management | 2026-07-24 | Implemented (10.0E-2) | — | phase-10.0e §23 |
| DEC-B54 | UX | **No /alerts route in 10.0E** — cockpit panel + counts suffice; a full workspace only with demonstrated need. | Management | 2026-07-24 | Approved | — | phase-10.0e §27 |
| DEC-B55 | UX | /dashboard alert volume = **8 primary + level counts + « N autres »**; engine cap 40. | Management | 2026-07-24 | Implemented (10.0E-3) | — | phase-10.0e §27 |
| DEC-B56 | Security | **Personal signals (my unread, my tasks) stay OUT of the tenant alert center** — separate composition; never shown tenant-wide. | Management | 2026-07-24 | Implemented | — | phase-10.0e §27 |
| DEC-B57 | Architecture | KPI `dossiers_intervention` (counts) and alert items share the **same cache()d Control-Tower/risk pass** — one risk doctrine; top-10 cap labeled honestly. | Management | 2026-07-24 | Implemented | — | phase-10.0e §27 |
| DEC-B58 | Data | **Unavailable alert sources surface as source-status (ok/unavailable/omitted)** — never « 0 alertes » for a dark source. | Management | 2026-07-24 | Implemented (10.0E-3) | — | phase-10.0e §27 |

---

## C. Commercial & finance strategy decisions

| Decision ID | Category | Decision | Owner | Date | Status | Related Blocker | Documents Impacted |
|---|---|---|---|---|---|---|---|
| DEC-C01 | Commercial | **Multi-Tenant Ready** (`tenant_id` + RLS from day one); **no SaaS control plane** built in P1 | Management | 2026-06-13 | Approved | Q32 | architecture.md, database-design.md |
| DEC-C02 | Commercial | Full SaaS commercialization (onboarding/billing/tenant admin) = **Phase 3, only if funded** | Management | 2026-06-13 | Proposed | Q32 | architecture.md |
| DEC-C03 | Finance | **Keep Sage**; **never rebuild accounting**; integrate (export billed files) in **Phase 2** | Management | 2026-06-13 | Approved | R1, R3 | audit.md, requirements.md |
| DEC-C04 | Finance | **Replace Maya** with native billing in **Phase 2**; untouched in Phase 1 | Management | 2026-06-13 | Approved | — | requirements.md, phase-1-roadmap.md |

### Phase 11.0 — Autorisation de Dépenses & Bon de Dépenses series (proposed 11.0A 2026-07-25; business clarifications ratified same day)

| Decision ID | Category | Decision | Owner | Date | Status | Related Blocker | Documents Impacted |
|---|---|---|---|---|---|---|---|
| DEC-C05 | Architecture | **Two separate entities** — `expense_authorization` + `expense_voucher`, own tables/state machines; optional `finance_request` link (never a replacement). | Management | 2026-07-25 | Proposed | — | docs/finance/phase-11.0a-expense-authorization-architecture.md |
| DEC-C06 | Workflow | **Every Bon derives from an APPROVED Autorisation** — the Bon cannot be created before approval; copied fields keep provenance + version history. | Management | 2026-07-25 | **Approved** (business clarification 2026-07-25) | — | phase-11.0a §8 |
| DEC-C07 | Workflow | **Mandatory ONE-TO-ONE relationship** — one Autorisation produces exactly ONE Bon (zero-or-one before payment prep); **no split vouchers, no consolidated vouchers** *(modifies the 11.0A audit's multi-Bon partial-payment recommendation)*; enforced by a unique authorization reference on the voucher. | Management | 2026-07-25 | **Approved AS MODIFIED** (business clarification 2026-07-25) | — | phase-11.0a §8 |
| DEC-C08 | Workflow | Autorisation visa chain strictly sequential (Demandeur→Chef Transit→Coordonnateur→Opération→Trésorière→DAF→DG) by default; parallelism only on explicit business statement. | Management | 2026-07-25 | Proposed | BLK-FIN-2 (Opération signer unmapped) | phase-11.0a §6 |
| DEC-C09 | Workflow | Bon six-visa chain strictly sequential (Agent→Réception→Comptable→DAF→DGA→DG); payment gated on all six on the same current version. | Management | 2026-07-25 | Proposed | BLK-FIN-1 (Réception signer unmapped) | phase-11.0a §6 |
| DEC-C10 | Finance | Payment method is part of the signed Bon; change after approval ⇒ controlled amendment + re-approval from the first affected visa; execution records the actual method. | Management | 2026-07-25 | Proposed | — | phase-11.0a §22 |
| DEC-C11 | Security | Visa→role mapping: Chef de Transit→CHIEF_OF_TRANSIT, Coordonnateur→COORDINATOR, DG→CEO; **VISA_AGENT → the Finance Agent boundary (FINANCE_OFFICER) — NEVER the CASHIER (ratified 2026-07-25; Cashier stays execution-only)**; new roles TREASURER/DAF/DGA/ACCOUNTANT created in 11.0B; **VISA_RECEPTION and VISA_OPERATIONS remain explicitly UNMAPPED business blockers** — no guessing, no placeholder grants. | Management | 2026-07-25 | **Partially approved** (VISA_AGENT ratified; two mappings open) | **BLK-FIN-1** (VISA_RECEPTION) · **BLK-FIN-2** (VISA_OPERATIONS) | phase-11.0a §4/§30; rbac-matrix.md |
| DEC-C12 | Security | Signature standard = **authenticated electronic approval** (immutable visa row: document+version+step+signer+role-at-signing+decision+timestamp+sha256+audit); NOT claimed as qualified e-signature pending legal review. | Management | 2026-07-25 | Proposed | DEC-C24 (legal) | phase-11.0a §5 |
| DEC-C13 | Workflow | Material edit ⇒ new document version ⇒ visas from the first affected step invalidated (rows immutable, new rows required); signed PDFs never overwritten. | Management | 2026-07-25 | Proposed | — | phase-11.0a §7 |
| DEC-C14 | Data | Document numbers minted **at submission** via the counter-RPC pattern (`EFT-AUT-YYYY-#####` / `EFT-BON-YYYY-#####`), per tenant, yearly reset, never reused; drafts unnumbered. | Management | 2026-07-25 | Proposed | — | phase-11.0a §12 |
| DEC-C15 | Data | **General non-dossier expenses allowed** — `file_id` nullable on the new entities; finance-only visibility for general expenses. | Management | 2026-07-25 | Proposed | — | phase-11.0a §13 |
| DEC-C16 | Architecture | PDF strategy = **Option A**: original template page raster as full-page background + calibrated coordinate overlays, enabled by ONE engine addition (JPEG Image XObject in `lib/reports/pdf.ts`); master template PDF committed to the repo first. | Management | 2026-07-25 | Proposed | template asset intake | phase-11.0a §9 |
| DEC-C17 | UX | Draft/partial/rejected/cancelled PDFs watermarked; final approved copy clean except a verification footer. | Management | 2026-07-25 | Proposed | — | phase-11.0a §15 |
| DEC-C18 | Architecture | QR verification **deferred to 11.0G** (fidelity first); card-token pattern (CSPRNG + uniform 404) when added. | Management | 2026-07-25 | Proposed | — | phase-11.0a §16 |
| DEC-C19 | UX | Visa-box rendering = « Approuvé » mark + signer name + date; optional signature image later; the ledger row is always the authority. | Management | 2026-07-25 | Proposed | — | phase-11.0a §17 |
| DEC-C20 | Security | Delegation (acting DAF/DGA/DG) **deferred to 11.0G**; when built: explicit, time-bounded, role-bound, audited — never “any admin may sign”. | Management | 2026-07-25 | Proposed | — | phase-11.0a §20 |
| DEC-C21 | Security | Approval vs execution separated: `finance:expense:sign` (visa roles) vs `finance:expense:execute` (CASHIER, execution-only). | Management | 2026-07-25 | Proposed | — | phase-11.0a §25 |
| DEC-C22 | Data | Supporting documents in a **dedicated finance-classified attachment table + private storage path** — never the dossier-bound `document` table. | Management | 2026-07-25 | Proposed | — | phase-11.0a §14 |
| DEC-C23 | Architecture | **Dedicated finance state machine** (pure transition table + CAS + identity maker-checker + append-only visa ledger) — the generic workflow engine is registry-fixed and dossier-welded. | Management | 2026-07-25 | Proposed | — | phase-11.0a §3 |
| DEC-C24 | Data | Archive-not-delete, checksum-verified signed artifacts, logical cancellation; **Senegal/OHADA retention + e-signature status require external legal verification before 11.0G**. | Management | 2026-07-25 | Proposed | legal review | phase-11.0a §24 |
| DEC-C25 | Data | **Field semantics ratified (business clarification 2026-07-25):** `account_number` = flexible free text (no bank/GL/sequence assumption; rendered exactly as entered on both templates) · `type` = flexible text in 11.0B (no hardcoded enum; validation boundary designed so a controlled tenant catalog can arrive later WITHOUT replacing historical values) · `weight_kg` = optional non-negative decimal, kilograms implicit, PDF renders the KG unit, absent for non-cargo expenses. | Management | 2026-07-25 | **Approved** | — | phase-11.0a §11 |
| DEC-C26 | Documents | **Master-template conflict + resolution (DEC-C16 layering).** The authoritative paper-template asset was named the first 11.0B prerequisite (11.0A §8) but was **never committed**; the 11.0C mission's premise that it had been is factually incorrect (verified 2026-07-26). Ratified resolution: build the full coordinate engine now, with the raster background as a **registered but empty slot** — the renderer draws (1) background raster when registered, (2) form chrome from the coordinate map as a stand-in, (3) values from the SAME map. Registering the scan turns layer 1 on and layer 2 off with **no value coordinate moving**, so recalibration stays a data edit in `lib/finance/expense/template-map.ts`. **Still owed before the fidelity claim is « pixel-identical »:** commit + checksum the master scan, calibrate against it, and run the 11.0A §29 pixel-diff + Finance sign-off acceptance. Until then the claim is « structurally faithful — every paper field, in the paper's order, A4 at 1:1 ». | Management | 2026-07-26 | **Approved** | DEC-C16 | phase-11.0c §1 |
| DEC-C27 | Workflow | **Draft-edit versioning rule.** A single entry point (`saveExpenseAuthorization`) owns it so the UI cannot diverge: BEFORE the first frozen version the working head is updated **in place** (CAS on status) — a draft carries no number and no version, per DEC-C14's invoice-at-issuance precedent, so an author's typing does not mint history; ONCE a version exists (the RETURNED correction path) every save is routed to the existing `createExpenseAuthorizationVersion`, freezing a NEW immutable version and superseding any open approval attempt per DEC-C13. No freeze/supersede logic is duplicated. Frozen versions now also record their template provenance (`template_code` + `template_version`). | Management | 2026-07-26 | **Approved** | DEC-C13, DEC-C14 | phase-11.0c §2 |
| DEC-C28 | Documents | **« Montant en lettres » is DERIVED, never client-supplied.** `amountInWordsFr(amount, currency)` is the single source consumed by the form preview, the stored head/version snapshot and the PDF; `amountInWords` was REMOVED from the server action's input contract so a changed amount can never leave stale words on a payment document. Implements the 11.0A §11 classification D. XOF spells no centimes; an unknown currency code is echoed rather than given an invented denomination name. Related: « N° dossier » is entered as the human `file_number` and resolved tenant-scoped to `file_id`; an unknown number is REJECTED (`unknown_file`), an empty one clears the link (DEC-C15 general administrative expense). | Management | 2026-07-26 | **Approved** | DEC-C15, DEC-C25 | phase-11.0c §2 |
| DEC-C29 | Documents | **Supporting documents implemented as ratified (DEC-C22):** dedicated `expense_attachment` table + dedicated private `finance-expense` bucket (deny-by-default, no `storage.objects` policies for `authenticated`), service-role-mediated upload, 60-second signed download URLs minted per request, tenant-partitioned UUID object keys — NEVER the dossier-bound `public.document` (its `file_id NOT NULL` cannot carry a general expense and its RLS would expose supplier quotes to every dossier reader). Attachments are **retired, never deleted**, may only change while the parent document is editable, and the table already carries the `voucher_id` parent + one-parent CHECK so 11.0D reuses it with no schema change. `kind` is flexible text (the required-evidence matrix remains an open business question). | Management | 2026-07-26 | **Approved** | DEC-C22, DEC-B19 | phase-11.0c §2 |
| DEC-C30 | Workflow | **11.0D chain conflict + resolution.** The 11.0D brief specified an EIGHT-step chain (Agent → Réception → Opération → Accountant → Treasurer → DAF → DGA → DG) which audit showed to be a MERGE of the two ratified chains: four steps exclusive to the *Bon* (DEC-C09), two exclusive to the *Autorisation* (DEC-C08), and three ratified Autorisation steps dropped. Blocking because (a) the paper form has SEVEN visa areas (11.0A §11) already printed by 11.0C, and an 8-step chain cannot be signed on the real document; (b) the brief's chain places BOTH unbound signers at positions 2–3, making APPROVED permanently unreachable; (c) `READY_FOR_PAYMENT` is a VOUCHER status (DEC-C21, "approval is never payment"), not an Authorization one. **RESOLVED: implement the ratified 7-step Autorisation chain (DEC-C08); terminal states APPROVED / REJECTED.** Signer map = code (lib/finance/expense/visa.ts), per the registry idiom; every eligibility decision is one pure total function shared by the action, the timeline and the queue. | Management | 2026-07-26 | **Approved** | DEC-C08, DEC-C09, DEC-C21 | phase-11.0d §1 |
| DEC-C31 | RBAC | **Visa signer grants + concurrency backstop.** `finance:expense:sign` (in the catalog since 11.0B, granted to nobody) goes to exactly the six seats that sign THIS chain — FINANCE_OFFICER (identity-bound Demandeur), CHIEF_OF_TRANSIT, COORDINATOR, TREASURER, DAF, CEO — plus `finance:expense:read` to CHIEF_OF_TRANSIT/COORDINATOR/CEO, who could not otherwise see what they sign. **NOT granted:** CASHIER (execution-only, DEC-C21) · SYSTEM_ADMIN (finance convention — an administrator must never manufacture an approval) · ACCOUNTANT/DGA (they sign the *Bon's* chain; withheld until wired) · nothing for VISA_OPERATIONS (BLK-FIN-2). **Governance:** the DG visa is CEO's first write-class finance capability — the change 11.0A §4 flagged to surface. **Backstop refinement:** the approved unique index `(authorization_id, version_id, step_ordinal)` would have blocked the legitimate correction path (a fresh attempt re-collecting step 1 on the same version); shipped as **`(attempt_id, step_ordinal)`** — same no-double-sign guarantee on the append-only ledger, correction path preserved, and it covers the Bon's chain for free. | Management | 2026-07-26 | **Approved** | DEC-C11, DEC-C12, DEC-C21 | phase-11.0d §2 |

---

## D. How to use this register
1. **Every decision** (new or changed) is recorded here first, then propagated to the impacted docs listed in its row.
2. To **change** an Approved decision: add a new row with a successor ID, set the old row's Status to `Superseded`, and note the successor.
3. When a **workshop closes a blocker**, update the matching `DEC-B*` row: fill the Date, set Status to `Approved`, finalize the decision text, then update the impacted docs and the [readiness checklist](s0-readiness-checklist.md).
4. The **Related Blocker** column ties each decision back to the [Critical Blockers Register](s0-readiness-checklist.md#critical-blockers-register).

---

## E. Decision summary by status

| Status | Count | IDs |
|---|---|---|
| Approved | 16 | DEC-A01–A12, DEC-B06, DEC-C01, C03, C04 |
| Proposed (default in force) | 9 | DEC-B01, B04, B05, B08, B13, B15, B16, B17, C02 |
| Implemented | 0 | — (no code built yet) |
| Superseded | 0 | — |
| Rejected | 0 | — |
| `Open` (intake — pre-lifecycle) | 10 | DEC-B02, B03, B07, B09, B10, B11, B12, B14, B18, B19 |

**Total decisions tracked: 35** (+ 11 intake items awaiting formulation).

> **The 4 hard S0 gates live in this register as:** DEC-B01 (BLK-1), DEC-B03 (BLK-3), DEC-B06 (BLK-6), DEC-B09 (BLK-9). When all four reach `Approved`, S0's blocker-dependent work is cleared to start.

---

## F. Change Control Process

Every change to a decision follows one of four paths. **No assumption, requirement, or design in any downstream document may change until the corresponding decision here is updated first.**

### F.1 New Decision
1. Allocate the next ID in the appropriate series (`DEC-A##` architecture/product · `DEC-B##` blocker-driven · `DEC-C##` commercial/finance).
2. Record: Category, Decision text, Owner, Date, Status = `Proposed`, Related Blocker, Documents Impacted (use [§H](#h-document-dependency-matrix)).
3. On ratification, move Status → `Approved` and propagate to every impacted document.

### F.2 Modified Decision (same intent, refined detail)
1. Edit the decision text **in place**, append `(rev N, YYYY-MM-DD)` to the Date cell.
2. Keep the same ID and Status (unless ratification level changes).
3. Update impacted documents and note the revision in the [readiness checklist](s0-readiness-checklist.md) if a gate is affected.

### F.3 Superseded Decision (intent changes)
1. Create a **new** decision row with a new ID capturing the new intent.
2. Set the old row's Status → `Superseded` and add `→ superseded by DEC-XXX` to its Decision cell.
3. Record date + owner on the new row; propagate to all impacted documents.
4. Never delete the old row — supersession preserves the audit trail.

### F.4 Emergency Decision (time-critical, made outside a workshop)
1. May be recorded directly with Status = `Approved` **only** by the Project Sponsor or IT Lead.
2. Must include `EMERGENCY` in the Notes/Decision cell and the deciding person's name.
3. Must be **ratified retroactively** at the next workshop; if not ratified, it is set to `Rejected` and reversed.
4. Log it in the readiness checklist's accepted-risk section until ratified.

| Change type | New ID? | Old row status | Who approves | Audit trail |
|---|---|---|---|---|
| New | Yes | — | Workshop owner | New row |
| Modified | No | unchanged | Decision owner | Revision tag on Date |
| Superseded | Yes (successor) | `Superseded` | Workshop owner | Old + new rows linked |
| Emergency | Yes | — | Sponsor / IT Lead | `EMERGENCY` tag + retro-ratify |

---

## G. Decision Status Standard

The canonical lifecycle for every decision ID. Status flows **Proposed → Approved → Implemented**, with **Superseded** and **Rejected** as terminal off-ramps. `Open` is a pre-lifecycle intake marker used only in the [Blocker Register](s0-readiness-checklist.md#critical-blockers-register).

| Status | Meaning | Entry condition | Who sets it |
|---|---|---|---|
| **Proposed** | A decision (often a documented default) is on the table, **not yet ratified**. Downstream docs may reference it but flag it as provisional. | Recorded in the register | Author / facilitator |
| **Approved** | Ratified and **binding**. Downstream documents must conform. Not yet built. | Workshop sign-off / sponsor approval | Decision owner |
| **Implemented** | The approved decision is **realized in code/config** and verified. | Merged + verified in the platform | Dev/IT lead |
| **Superseded** | Replaced by a newer decision. **Retained** for audit; points to its successor. | A successor decision is Approved | Workshop owner |
| **Rejected** | Considered and **declined** (incl. un-ratified emergency decisions). Retained for the record. | Explicit rejection | Decision owner / sponsor |

> `Open` (intake): a blocker exists but no decision has been formulated yet — not part of the formal lifecycle. It becomes `Proposed` the moment a candidate decision (even a default) is written into the register.

---

## H. Document Dependency Matrix

Which planning/governance documents must be updated when a decision of each **category** changes. Use this to fill the "Documents Impacted" column and to know what to propagate after a change. ✅ = primary impact (must review/update) · ▫ = secondary (check for consistency).

| Decision category | requirements | state-machine | document-catalog | database-design | rbac-matrix | architecture | phase-1-roadmap | s0-readiness | workshops |
|---|---|---|---|---|---|---|---|---|---|
| **Architecture** | ▫ | ▫ | | ✅ | ▫ | ✅ | ▫ | ▫ | |
| **Security** | ▫ | | | ✅ | ✅ | ✅ | | ▫ | ▫ |
| **Workflow** | ✅ | ✅ | ▫ | ✅ | ▫ | | ▫ | ▫ | ✅ |
| **Documents** | ✅ | ▫ | ✅ | ✅ | | | | ▫ | ✅ |
| **Hosting** | | | | ▫ | | ✅ | ▫ | ✅ | ✅ |
| **Integrations** | ✅ | ▫ | | ✅ | | ✅ | ▫ | ▫ | ✅ |
| **Notifications** | ✅ | ▫ | | ✅ | | ▫ | ▫ | | ✅ |
| **Portal** | ✅ | | ▫ | ✅ | ✅ | ▫ | ▫ | | |
| **RBAC** | ▫ | ▫ | | ✅ | ✅ | ▫ | | ▫ | ▫ |

**Category → decision-series hint:** Architecture/Hosting/Integrations/Portal → `DEC-A*`/`DEC-B*` · Security/RBAC → `DEC-B*` (RB/DB series) · Workflow/Documents/Notifications → `DEC-A*`/`DEC-B*` · Commercial/Finance → `DEC-C*`.

> **Rule:** after any decision changes status, open every ✅ document in its category row, apply the change, and confirm the ▫ documents remain consistent. Record completion in the change's row (Date cell).
