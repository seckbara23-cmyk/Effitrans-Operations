/**
 * Platform (SaaS product) branding (Phase 4.0B-3). PURE constant.
 * ---------------------------------------------------------------------------
 * Used for /platform surfaces AND as the ultimate safe fallback when a tenant has
 * no branding. This is the PRODUCT's brand — never a tenant's.
 */
export const PLATFORM_BRANDING = {
  displayName: "Effitrans Platform",
  primaryColor: "#0B1F33",
  secondaryColor: "#0F766E",
} as const;
